# Plan 06-01: Image Optimization and Lazy Loading

## Summary

Add native lazy loading to the ImageWithFallback component for improved performance.

## Detailed Steps

### Step 1: Update ImageWithFallback component

File: `src/app/components/figma/ImageWithFallback.tsx`

Add native `loading="lazy"` attribute to defer offscreen image loading:

```tsx
import React, { useState } from 'react'

const ERROR_IMG_SRC =
  'data:image/svg+xml;base64,'

interface ImageWithFallbackProps extends React.ImgHTMLAttributes<HTMLImageElement> {
  eager?: boolean // For above-the-fold images like hero
}

export function ImageWithFallback({ eager, ...props }: ImageWithFallbackProps) {
  const [didError, setDidError] = useState(false)

  const handleError = () => {
    setDidError(true)
  }

  const { src, alt, style, className, ...rest } = props

  return didError ? (
    <div
      className={`inline-block bg-gray-100 text-center align-middle ${className ?? ''}`}
      style={style}
    >
      <div className="flex items-center justify-center w-full h-full">
        <img src={ERROR_IMG_SRC} alt="Error loading image" {...rest} data-original-url={src} />
      </div>
    </div>
  ) : (
    <img
      src={src}
      alt={alt}
      className={className}
      style={style}
      loading={eager ? 'eager' : 'lazy'}
      {...rest}
      onError={handleError}
    />
  )
}
```

### Step 2: Mark hero image as eager

File: `src/app/components/Hero.tsx`

Add `eager` prop to the hero image so it loads immediately (above the fold):

```tsx
<ImageWithFallback
  eager
  // ...existing props
/>
```

## Verification

- [ ] Build passes: `npm run build`
- [ ] Portfolio images load lazily when scrolling
- [ ] Hero image loads immediately
- [ ] No console errors
